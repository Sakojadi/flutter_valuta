import 'package:flutter/material.dart';


class Events extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Events Window'),
      ),
      body: Center(
        child: Text('Data added will be displayed here'),
      ),
    );
  }
}

